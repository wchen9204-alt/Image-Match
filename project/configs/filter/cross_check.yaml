# ---------------------------------------------------------------------------
# 交叉检查过滤器配置
# 对 top-1 最近邻执行双向一致性检查。
# ---------------------------------------------------------------------------
type: CROSS_CHECK

params:
  # true 时要求 query 和 train 描述子互为最近邻；false 时直接放行。
  enabled: true
