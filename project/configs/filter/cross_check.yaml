# ---------------------------------------------------------------------------
# 交叉检查过滤器配置
# 对 top-1 最近邻执行双向一致性检查
# ---------------------------------------------------------------------------
type: CROSS_CHECK

params:
  # 是否启用该过滤器，默认值: true
  enabled: true
