# ---------------------------------------------------------------------------
# 绝对距离阈值过滤
# 直接保留 distance <= max_distance 的匹配
# ---------------------------------------------------------------------------
type: DISTANCE_THRESHOLD

params:
  max_distance: 30.0
