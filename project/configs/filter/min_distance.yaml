# ---------------------------------------------------------------------------
# Min Distance 过滤
# 距离过滤下限：distance <= max(multiplier * min_dist, min_cutoff)
# ---------------------------------------------------------------------------
type: MIN_DISTANCE

params:
  multiplier: 2.0
  min_cutoff: 30.0
