# ---------------------------------------------------------------------------
# Lowe 比值检验过滤器配置
# ---------------------------------------------------------------------------
type: RATIO_TEST

params:
  # 比值阈值：当 d1 / d2 < ratio 时保留匹配。
  # 常用范围：SIFT/SURF 为 0.7~0.8，二进制描述子为 0.75~0.85。
  ratio: 0.75
